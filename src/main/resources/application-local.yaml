# Server / Port configuration
server:
  port: 8888

spring:

  # Data source configurations with database driver class
  datasource:
    url: jdbc:postgresql://localhost:5432/teachhive
    username: postgres
    password: linux
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20     # Hikari Connection Pool size maximum of 20 connections permitted

  # JPA/ORM & SQL configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true
    open-in-view: false

  # Predefined configuration for Multipart form data uploads
  servlet:
    multipart:
      max-request-size: 40MB
      max-file-size: 40MB

# Indication for Spring Context to include working through SMTP mailing service
  mail:
    # Which host we use
    host: smtp.gmail.com
    # The port through which mailing messages are sent
    port: 587
    # My email address as a producer
    username: doniyork334@gmail.com
    # App password (!Sensitive data)
    password: "ujxr khbu ognm wsmn"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# Logging configurations for further details throughout the execution flow
logging:
#  level:
#    org.example.server: DEBUG
#    org.springframework.security: DEBUG
#    org.springframework.mail: DEBUG
  pattern:
    # Logging pattern for printing out (Dragged from AI)
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID}){magenta} --- %clr(%logger{36}){cyan} : %m%n"

  # JWT Security configuration with key & expiration mills
application:
  security:
    jwt:
      secretKey: YWQzMmQ5ZDE4MDk5ODI2ZTM5ZjliYTQ1MjMwOGRjMWExODEwN2UzMjI1N2U5ZjgxOWJkN2Y0OGI0ZTE5YjNhZDkxNzNhZTFjZjg3NDg1MzVlYzdkMTU5YjQ5Njc2OTQx
      jwtExpirationMs: 86400000
